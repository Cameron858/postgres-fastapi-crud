[project]
name = "ui_lib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Cameron", email = "cameronwood858@googlemail.com" }]
requires-python = ">=3.13"
dependencies = ["dash>=4.4.1", "gunicorn>=26.2.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = ["mypy>=2.3.0", "pre-commit>=4.6.0", "ruff>=0.15.22"]
